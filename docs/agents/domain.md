# Domain Docs

Engineering skills should read `CONTEXT.md` at the repo root and relevant ADRs under `docs/adr/` before exploring the codebase.

This is a single-context repository. Use the glossary vocabulary defined in `CONTEXT.md`, and explicitly flag conflicts with existing ADRs.
